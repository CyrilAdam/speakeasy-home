services:
  backend:
    build:
      context: backend
      dockerfile: Dockerfile
    restart: unless-stopped
    environment:
      NODE_ENV: production
      PORT: "3001"
      DB_PATH: /data/speakeasy.db
    volumes:
      - db-data:/data

  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    depends_on:
      - backend
    networks:
      - default
      - traefik
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.speakeasyhome.rule=Host(`speakeasyhome.stacklab.be`)"
      - "traefik.http.routers.speakeasyhome.entrypoints=websecure"
      - "traefik.http.routers.speakeasyhome.tls.certresolver=letsencrypt"
      - "traefik.http.services.speakeasyhome.loadbalancer.server.port=80"
      - "traefik.docker.network=budgetmanager_budgetmanager-network"

volumes:
  db-data:

networks:
  traefik:
    external: true
    name: budgetmanager_budgetmanager-network
